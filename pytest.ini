[pytest]
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    regression: marks tests as regression tests (pixel-perfect comparisons, run manually)

# Skip slow tests by default for faster iteration
# To run ALL tests including slow ones: pytest -m ""
# To run ONLY slow tests: pytest -m slow
addopts = -m "not slow"
