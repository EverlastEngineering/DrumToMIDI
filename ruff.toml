# Ruff configuration
# https://docs.astral.sh/ruff/configuration/

# Exclude archived code and external libraries from linting
exclude = [
    "archive/",
    "lib_v5/",  # External MDX model code
    "__pycache__",
    ".pytest_cache",
    "*.egg-info",
]

line-length = 120
